// success text
setTimeout(() => {
    // 👇️ hiding the logged in status after 5 seconds
  const box = document.getElementById('successTxt');

  // 👇️ removes element from DOM
  box.style.display = 'none';
}, 3000); 

// success text
setTimeout(() => {
    // 👇️ hiding the logged in status after 5 seconds
  const box = document.getElementById('deleteTxt');

  // 👇️ removes element from DOM
  box.style.display = 'none';
}, 3000); 

// function hide(){
//   document.querySelector('#searchBar').setProperty("display", "none", "important");
//   // document.querySelector('#searchBar').style.display='none';
// }